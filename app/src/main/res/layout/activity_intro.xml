<?xml version="1.0" encoding="utf-8"?>
<layout>

    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginBottom="@dimen/_3sdp">

            <androidx.viewpager2.widget.ViewPager2
                android:id="@+id/info_pager"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                app:layout_constraintBottom_toTopOf="@+id/next_done_txt"
                app:layout_constraintTop_toTopOf="parent" />

            <ImageView
                android:id="@+id/indicator"
                android:layout_width="141px"
                android:layout_height="35px"
                android:layout_marginStart="@dimen/_10sdp"
                app:layout_constraintBottom_toBottomOf="@+id/next_done_txt"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="@+id/next_done_txt" />

            <TextView
                android:id="@+id/next_done_txt"
                android:layout_width="242px"
                android:layout_height="114px"
                android:fontFamily="@font/gilroy_semi_bold"
                android:gravity="center"
                android:lines="1"
                android:textColor="@drawable/txt_selector"
                android:textSize="@dimen/_15sdp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />
        </androidx.constraintlayout.widget.ConstraintLayout>
    </RelativeLayout>
</layout>